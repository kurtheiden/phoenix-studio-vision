# OS1 durable helper: technical and operating-boundary review

Date: 2026-09-20. **Technical verdict: ACCEPTABLE FOR SEPARATE OPERATOR APPROVAL**, limited to this exact executable's metadata-only role under the controls below. **Authentic reserve intake remains BLOCKED.** This review is neither independent human sign-off nor authorization to supply a reserve path or run the helper on authentic material.

## Reviewed identity and evidence

At review start, `HEAD`, `main`, and `origin/main` were `584e8eb041c78ddd089411daf1db30877e8d0a76`, branch `main`, staging empty. The existing modified `docs/DECISIONS.md` and `docs/ROADMAP.md` and two untracked local research documents were left untouched; all four matched their recorded SHA-256 values. The governing [blind-validation protocol](OS1_BLIND_VALIDATION_PROTOCOL.md), subordinate [reserve-intake protocol](OS1_RESERVE_INTAKE_PROTOCOL.md), [independent review](OS1_METADATA_HELPER_INDEPENDENT_REVIEW_2026_09_19.md), [operator checklist](OS1_MINIMUM_0_1_BLIND_INTAKE_OPERATOR_CHECKLIST.md), [durable candidate record](OS1_METADATA_HELPER_DURABLE_BINARY_REVIEW_CANDIDATE.md), helper source/build/test scripts, and acceptance runner/README were reviewed.

The reviewed source, build script, test script, and helper test source retain the candidate record's SHA-256 values `55976c0c758a6c29799d94afce472f8cfccd5e8886b04f932098841b4e2d89ef`, `ad83093d37fe70b2b153f8a245f4fe01c739f48b3434afb14a22f6655d6c6c56`, `bd431c83a1aa3b01540c6164ca63bf365bc6be42b41aded04061a1ac26bc529a`, and `f381b508dc764b90674ab3ba721924276c79d079102c93277fb766f069201568`. Acceptance runner SHA-256: `16f1ce215e3100012cfb469e31f804e120b31a6dad3019b2535da8e2934dce6c`. The prior documented build used Apple Swift 6.2.4 on macOS 15.7.5; focused synthetic results were 57 pass, 0 fail, 1 unavailable, plus 12 acceptance unit tests passing. The same-basename comparison matched bytes on that toolchain. These are recorded results; no build, helper command, or test was rerun for this review.

The durable executable is `/Users/kurtheiden/.local/share/phoenix-os1-review-candidates/2026-09-20/phoenix-os1-metadata-helper`, SHA-256 `0ca58226b7797306f02dc1b4a5ed267f61beda6d0c106e694f52b12c84fb8899`. Read-only checks found a regular 136280-byte file, owner UID 501, mode `0700`; its real path equals its stated path. No ancestor is a symlink. The immediate review directory is `0700`; its parent and `~/.local/share` are `0755`. Thus the binary is not publicly readable or executable by mode, but the wider parent chain is traversable. The path is on the local Data volume and outside the repository and named cloud roots. Whether a separate sync, backup, or security agent handles it is **not established**; an operator must verify the actual service boundary and access policy before authentic use. Rehash the same regular executable immediately before each separately authorized command and preserve the independent digest.

## Technical scope and findings

Source inspection supports immediate-child, no-follow enumeration; descriptor-bound regular-file inspection; bounded Finder Type/Creator and resource-fork presence observations; two complete data-fork SHA-256 passes; neutral IDs; private exclusive outputs; and fixed failure codes. The acceptance runner independently hashes frozen executable and MAP bytes, checks MAP identity and complete IDs/rows, runs `inspect`, compares mapped sources before/after, and hashes the saved RECORD. These controls support a narrowly bounded metadata-intake role when the external boundary is established.

Limits remain material: the overlapping in-read mutation/replacement race was unavailable in synthetic tests. An ordinary regular archive can be `INSPECTED`; neither helper nor runner classifies archives or disk images. A recomputed unkeyed MAP digest does not authenticate the MAP or bind it to the binary; an independently frozen saved-MAP digest does. The acceptance runner does not authenticate the preceding `enumerate` run, historical source identity, all ancestor/equivalent-path arrangements, candidate-only contents, reference quarantine, or the operator's roles. It executes a binary by path after reading its bytes, so path replacement between verification and execution is an operating risk unless the location and write access are controlled. Rejected acceptance may leave a partial RECORD; its generic rejection message alone does not retain the helper's command/exit/output, so the operator must capture failure evidence externally. No source or acceptance code change is proposed here.

## Frozen plan before any authentic path is supplied

1. Assign and record owner/provenance authority, metadata operator, independent human reviewer, and later reference reveal authority. The owner supplies private-validation permission, bounded provenance and Phoenix-exposure history, and affirmative **later** reference availability without opening a candidate or reference. Keep reference MIDI, exports, screenshots, notes, and associated audio outside the intake workspace and analyst access. Record the quarantine and incident contacts. The independent human must sign off on this exact binary/source/build/test identity, the unavailable coverage, and the concrete boundary; separately authorize the one actual execution.
2. Before giving the operator a root, obtain an owner attestation that it contains only prospective regular project artifacts, or establish a separately prepared candidate-only root/list without reading candidate contents. Exclude known archives, disk images, packages, Finder aliases, symlinks, directories, reference exports, media, screenshots, notes, and all other noncandidate objects. Any unresolved object is excluded or refused without content inspection. Do not use magic, previews, Studio Vision, Phoenix, or reference inspection to classify it.
3. Privately choose actual local, non-cloud source and output paths **later**, outside the repository, with source and output physically separate. Before approval, independently inspect `realpath`, `lstat`/file kind, owner/mode and each ancestor of the executable, root, MAP, RECORD, expectations, acceptance output, and private evidence destination. Reject symlinked ancestors, aliases, equivalent-path ambiguity, cloud sync, public access, and output-under-source arrangements. Require private mode-`0700` output parents, restrictive `umask 077`, exclusive new destinations, and reviewed indexing/preview/logging controls. Record what can and cannot be guaranteed about backup, EDR, caches, and OS services. Do not place authentic paths in this repository document.
4. Freeze the repository/protocol versions and hashes, executable SHA-256, actual resolved paths, commands, roles, permissions, failure handling, and private evidence custody before any root is supplied. Use direct argv or a reviewed launcher with tracing and history disabled; retain exact argv and actual exit/output in private evidence without exposing paths publicly. The placeholders below are **not** approved paths.

## Command and evidence sequence after separate authorization only

Use the approved executable path above as `<FROZEN_BINARY>`. Let `<APPROVED_CANDIDATE_ONLY_ROOT>`, `<PRIVATE_MAP>`, `<PRIVATE_RECORD>`, `<PRIVATE_EXPECTATIONS_JSON>`, and `<PRIVATE_ACCEPTANCE_JSON>` be separately resolved, approved absolute paths. Destinations must not exist before their respective commands.

```text
<FROZEN_BINARY> enumerate --root <APPROVED_CANDIDATE_ONLY_ROOT> --map-output <PRIVATE_MAP>

python3 tools/os1-metadata-helper/acceptance/accept_inspect.py \
  --binary <FROZEN_BINARY> --map <PRIVATE_MAP> \
  --record-output <PRIVATE_RECORD> \
  --expectations <PRIVATE_EXPECTATIONS_JSON> \
  --acceptance-output <PRIVATE_ACCEPTANCE_JSON>
```

For `enumerate`, independently capture exact argv, actual exit status and bounded output, root identity, source observations, and saved MAP bytes/digest. Require exit `0`, expected neutral-ID sequence and count, approved root identity, and no unexpected output or change. Freeze the MAP's independent SHA-256 **outside** the MAP before creating the expectations JSON. That private JSON must contain exactly `executable_sha256` and `map_sha256`, computed independently from saved bytes, not copied from MAP self-reports. Freeze the JSON bytes/digest, executable bytes/digest, destinations, and acceptance-runner identity before `inspect`.

For accepted `inspect`, preserve the acceptance runner's recorded command and exit, the runner's own exit/output, saved MAP and RECORD bytes with independent SHA-256 values, acceptance JSON bytes/digest, complete row/ID accounting, each inspected row's verified source evidence or explicit refusal, and mapped-source evidence before/after. Independently reread saved evidence and confirm it remains unchanged. A saved file alone is never success. `INSPECTED` denotes metadata/hash intake, not Phoenix readiness or export eligibility.

On nonzero exit, `ERROR`, missing/partial row, source or metadata change, ambiguous identity/path, leakage, or suspected race, stop without silent retry. Preserve original MAP, any partial or altered RECORD, acceptance output if present, command/exit/bounded error, digests, before/after evidence, and a private incident/exposure account. Do not overwrite or delete failed evidence; independent human review decides whether any continuation is possible. Do not replace a candidate based on Phoenix behavior.

Metadata intake ends before cohort selection. Later use only permitted metadata and owner attestations to group logical families, exclude uncertainty, freeze the complete inventory and deterministic two-candidate/fallback order, and obtain independent selection review. A separate provenance lock and Phoenix-only observation require separate authorization. Freeze the complete Phoenix result before the reference holder reveals anything; only then compare and assess Gates 1/5. No candidate, owner attestation, authentic path, human approval, or execution authorization is asserted by this document.
